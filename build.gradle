

allprojects{
    
     buildscript {
        repositories { jcenter() }
    }
   
    apply plugin: 'idea'

    repositories {
        jcenter()
        maven {
            // Ow2 ASM repository
            url 'https://repository.ow2.org/nexus/content/repositories/releases'
        }
    }
}

subprojects{

    apply plugin: 'java'
}

